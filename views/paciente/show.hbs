<h2 class="text-center mb-4">Detalle del Paciente</h2>

<div class="card shadow-lg mx-auto" style="max-width: 700px;">
  <div class="card-header bg-primary text-white text-center">
    <h4>{{paciente.nombres}} {{paciente.apellidos}}</h4>
    <small>ID: {{paciente.idpaciente}}</small>
  </div>
  <div class="card-body">
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><strong>Login:</strong> {{paciente.login}}</li>
      <li class="list-group-item"><strong>Password:</strong> {{paciente.password}}</li>
      <li class="list-group-item"><strong>Móvil:</strong> {{paciente.movil}}</li>
      <li class="list-group-item"><strong>Correo:</strong> {{paciente.correo}}</li>
      <li class="list-group-item"><strong>Dirección:</strong> {{paciente.direccion}}</li>
    </ul>
  </div>
  <div class="card-footer text-end">
    <a href="/paciente" class="btn btn-secondary me-2">
      <i class="bi bi-arrow-left-circle"></i> Volver
    </a>
    <a href="/paciente/edit/{{paciente._id}}" class="btn btn-warning me-2">
      <i class="bi bi-pencil-fill"></i> Editar
    </a>
    <form action="/paciente/delete/{{paciente._id}}" method="post" class="d-inline">
      <button type="submit" class="btn btn-danger" onclick="return confirm('¿Deseas eliminar este paciente?');">
        <i class="bi bi-trash-fill"></i> Eliminar
      </button>
    </form>
  </div>
</div>
